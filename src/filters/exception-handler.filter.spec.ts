import { ExceptionHandlerFilter } from './exception-handler.filter';

describe('ExceptionHandlerFilter', () => {
  it('should be defined', () => {
    expect(new ExceptionHandlerFilter()).toBeDefined();
  });
});
